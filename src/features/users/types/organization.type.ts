/** Reference data for a user's job assignment (GET /api/departments, GET /api/positions). */
export type Department = {
  id: string
  name: string
}

export type Position = {
  id: string
  name: string
}
