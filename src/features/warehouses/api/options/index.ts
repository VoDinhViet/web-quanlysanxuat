// Every read in this feature — one queryOptions factory per file. Query key convention
// (see .claude/rules/architecture.md): `["warehouses"]` is the feature root.
export { warehouseOptionsQueryOptions } from "@/features/warehouses/api/options/warehouse-options.options"
